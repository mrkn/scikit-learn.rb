require "spec_helper"

describe Scikit::Learn do
  it "has a version number" do
    expect(Scikit::Learn::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
